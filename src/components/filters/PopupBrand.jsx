import * as S from './Filters.styles'

export const PopupBrand = () => {
  return (
    <S.FilterPopup style={{ left: '330px' }}>
      <S.PopupList>
        {/* {artists.map((artist) => {
            return ( */}
        <S.PopupText
        // key={artist}
        // onClick={() => handlerSetAuthors(artist)}
        // $activeFilter={artist && authorTrack.includes(artist)}
        >
          {/* {artist} */}
        </S.PopupText>
        {/* )
          })} */}
      </S.PopupList>
    </S.FilterPopup>
  )
}
